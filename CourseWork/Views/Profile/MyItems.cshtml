@model List<CourseWork.Models.Entities.Item>
@{
    ViewData["Title"] = "MyItems";
}

<h1>MyItems</h1>
<div>
    @if(Model.Count == 0)
    {
        <h1>Вы пока не создали ни одного айтема</h1>
    }
    @foreach(var item in Model)
    {
        <p>@item.Name</p>
    }
</div>

